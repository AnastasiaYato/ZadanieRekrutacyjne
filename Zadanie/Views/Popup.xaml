<Window x:Class="Zadanie.Views.Popup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zadanie.Views"
        mc:Ignorable="d"
        Title="Popup" Width="250" Height="200"
        ResizeMode="CanResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Viewbox>
        <TextBox Name="PopupText" Grid.Row="0" FontSize="10" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Margin="5" IsReadOnly="True" BorderThickness="0" TextWrapping="Wrap"/>
        </Viewbox>
            <Button Grid.Row="1" Margin="30" FontSize="10" Content="OK" Click="OKButton_Click" />
            
    </Grid>

</Window>
